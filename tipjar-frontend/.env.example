# Solana Program ID
# Replace with your deployed program ID after running: anchor deploy
VITE_PROGRAM_ID=YourProgramIdHere

# Solana Network
# Options: devnet, testnet, mainnet-beta
VITE_NETWORK=devnet

# RPC URL (Optional - uses public endpoints if not set)
# VITE_RPC_URL=https://api.devnet.solana.com

# For production/mainnet, use a private RPC provider:
# VITE_RPC_URL=https://your-private-rpc-endpoint.com
